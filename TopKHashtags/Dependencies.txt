1) If ran on VCL ,the application has a GUI made using java swing hence it will require XWIN32 installed and it should be running before executing the script run.sh.

2) JDK( java development kit) and JRE( java runtime environment) needs to be installed since the application is java based.